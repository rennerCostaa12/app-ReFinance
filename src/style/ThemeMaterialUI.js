import { createTheme } from '@mui/material/styles';

export const themeMaterial = createTheme({
    palette: {
        primary: {
            main: '#4932D6',
            contrastText: '#202020',
        },
        secondary: {
            main: '#CCF282',
            contrastText: '#202020',
        }
    },
});